{
  "name": "unu-raymi-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "install:backend": "npx -y pnpm install --prefix backend",
    "build:backend": "npx -y pnpm run build --prefix backend",
    "start:backend": "npx -y pnpm run start --prefix backend",

    "install:admin": "npx -y pnpm install --prefix admin",
    "build:admin": "npx -y pnpm run build --prefix admin",
    "start:admin": "npx -y pnpm run start --prefix admin -- -p $PORT",

    "install:frontend": "npx -y pnpm install --prefix frontend",
    "build:frontend": "npx -y pnpm run build --prefix frontend",
    "start:frontend": "npx -y pnpm run start --prefix frontend -- -p $PORT"
  }
}
